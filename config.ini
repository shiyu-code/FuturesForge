# 基础
use_backtest=true
use_ctp=false
enable_csv_logs=true
csv_dir=build/data_new

# 回测配置
backtest_file=data/ticks.csv
backtest_speed_ms=200
backtest_meta=data/meta.json
backtest_rules=data/config.json

# 账户与前置（回测下不使用，但保留）
md_front=stub
td_front=stub
broker_id=sim
user_id=sim
password=sim

# 订阅合约（为空表示订阅所有）
instruments=

# Bar配置
bar_interval_sec=1

# 风控
max_pos_per_instrument=2
max_orders_per_bar=2
min_order_interval_ms=100